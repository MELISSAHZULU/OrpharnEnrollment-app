import 'package:flutter/material.dart';

class GovernmentDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Government Dashboard - Coming Soon'),
      ),
    );
  }
}