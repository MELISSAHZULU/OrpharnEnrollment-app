import 'package:flutter/material.dart';

class SocialWorkerDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Social Worker Dashboard - Coming Soon'),
      ),
    );
  }
}